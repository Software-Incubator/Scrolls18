const Teams = require('../models/team');
const Members = require('../models/members');
const TeamDetails = require('../models/team_details');

// function resgitserteam(teamInfo) {
    
// }

module.exports = {
    registerTeam: function(req, res) {
        console.log('verified');
    }
}